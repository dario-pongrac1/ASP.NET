@model IReadOnlyList<Vehicle>

<div class="table-responsive">
    <table class="table paper-table">
        <thead>
            <tr>
                <th>Registracija</th>
                <th>Proizvodac / Model</th>
                <th>Godina</th>
                <th>Gorivo</th>
                <th>Vlasnik</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model.Count == 0)
            {
                <tr>
                    <td colspan="6" class="text-center text-muted">Nema rezultata pretrage</td>
                </tr>
            }
            else
            {
                foreach (var vehicle in Model)
                {
                    <tr>
                        <td>@vehicle.LicensePlate</td>
                        <td>@vehicle.Manufacturer @vehicle.Model</td>
                        <td>@vehicle.Year</td>
                        <td>@vehicle.FuelType</td>
                        <td>
                            <a asp-controller="Customers" asp-action="Details" asp-route-id="@vehicle.CustomerId">
                                @vehicle.Owner?.FirstName @vehicle.Owner?.LastName
                            </a>
                        </td>
                        <td><a asp-action="Details" asp-route-id="@vehicle.Id">Detalji</a></td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>